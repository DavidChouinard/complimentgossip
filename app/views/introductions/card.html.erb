<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Compliment Gossip</title>

  <style>
@font-face {
  font-family: 'Playfair Display';
  src: url('http://static.davidchouinard.com/fonts/PlayfairDisplay-Regular.ttf') format('truetype');
}
@font-face {
  /* This is extremely hacky (to deal with a bug in Lob's renderer).
    Orginal TTF font name is change to Playfair Display Italic and used a seperate font here */
  font-family: 'Playfair Display Italic';
  src: url('http://static.davidchouinard.com/fonts/PlayfairDisplay-Italic.ttf') format('truetype');
  font-style: italic;
}
@font-face {
  font-family: 'Playfair Display SC';
  src: url('http://static.davidchouinard.com/fonts/PlayfairDisplaySC-Regular.ttf') format('truetype');
}

html {
  box-sizing: border-box;
}

*, *:before, *:after {
  box-sizing: inherit;
}

body {
  width: 6.25in;
  height: 4.25in;
  margin: 0;
  padding: 0;
  font-family: 'Playfair Display', Georgia, serif;
}

#introduction_content {
  height: 64px;
}

.page {
  page-break-after: always;
  margin: 0.125in;
  padding: 0.25in;
  height: 100%;
  /*border: 1px solid #D3D3D3;*/
  text-align: center;
}

.page-back-intro {
  font-style: italic;
  /* ugly hack alert */
  font-family: 'Playfair Display Italic', Georgia, serif;
  color: #4f4f4f;
  font-size: 12px;
  margin: 0.25in 1in;
}

.page-back-link {
  font-size: 14px;
  margin-top: 1in;
}

</style>

<%= stylesheet_link_tag "bigcard" %>
<%= stylesheet_link_tag "templates" %>
</head>
<body>

  <div class="page bigcard" id="<%= @introduction.template ? "template-#{@introduction.template}" : "" %>">
    <div class="tip"></div>
    <div id="introduction_content">“<%= @introduction.content %>”</div>
    <div id="introduction_sender_name"><%= @sender.name %></div>
  </div>

  <div class="page page-back" id="<%= @introduction.template ? "template-#{@introduction.template}" : "" %>">

    <div class="page-back-intro">Welcome to the halls of Compliment Gossip. Here, everyone recognizes each other for the greatness they bring to the world.</div>

    <div class="page-back-link">http://complimentgossip.com/<%= @introduction.key %></div>

  </div>

</div>
</body>
</html>
